#include <iostream>
using namespace std;

int main()
{
    /*
    주소에 *을 붙이면 값이 되고,
    값에 &을 붙이면 주소가 된다.
    */

   /*
   동적 할당. 더블포인터(이중배열)
   함수포인터

   배열선언(우리가 지금까지 배운 것)은 정적 할당이다.

   int a[50] = 딱 50개가 만들어진다.
   */
}